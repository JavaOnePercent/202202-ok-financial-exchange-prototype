group = "ru.otus.otuskotlin.financial.exchange.prototype"
version = "0.0.1"

subprojects {
    group = rootProject.group
    version = rootProject.version

    repositories {
        mavenCentral()
    }
}